import { SpacesDirective } from './spaces.directive';

describe('SpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new SpacesDirective();
    expect(directive).toBeTruthy();
  });
});
